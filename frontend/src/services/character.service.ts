export const saveCharacter = async (characterData: any) => {
  // Logic to save character to backend
  console.info("Saving character:", characterData);
};

export const getCharacters = async () => {
  // Logic to fetch saved characters
  return [];
};
