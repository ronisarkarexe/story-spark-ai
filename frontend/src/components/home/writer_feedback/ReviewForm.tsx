import React from 'react';

export const ReviewForm = () => {
  return <div>Review Form Bypassed</div>;
};

// Exporting both ways just in case they imported it differently elsewhere!
export default ReviewForm;